program while_test

{

    int foo() int cnt[]; {
        cnt = new int[2];
        while(cnt[0] < 3)) {
            print('0', 4); print(':',1); print(cnt[0],1); print(eof);

            while(cnt[1] < 3)) {
                print('1',4); print(':',1); print(cnt[1],1); print(eof);

                if (cnt[1] == 1) return 5;;
                cnt[1]++;

            }
            cnt[0]++;
        }
    }


    void main() int cnt[], f; {
        cnt = new int[5];

        while(cnt[0] < 3)) {
            print('0', 4); print(':',1); print(cnt[0],1); print(eof);

            while(cnt[1] < 3)) {
                print('1',4); print(':',1); print(cnt[1],1); print(eof);

                if (cnt[1] == 1) break;

                cnt[1]++;

            }

            print('2', 4); print(':',1); print(cnt[0],1); print(eof);

            cnt[2] = cnt[0];

            while(cnt[2] < 3)) {
                print('2',4); print(':',1); print(cnt[2],1); print(eof);

                cnt[3] = cnt[2];

                while(cnt[3] < 4)) {
                    print('3',4); print(':',1); print(cnt[3],1); print(eof);
                    cnt[3]++;
                    if (cnt[3] < 2 ) continue;
                    else {
                        cnt[3] = cnt[3] + 12;
                        break;
                    }
                }
            
                print('3',4); print('b',1); print(':',1); print(cnt[3],2); print(eof);
                cnt[2]++;
            }

            cnt[0]++;
        }

        f = foo();
        print(eof);
        print(f);
    }
}